<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <script src="https://kit.fontawesome.com/42a73ceb0f.js" crossorigin="anonymous" ></script>
        <link rel="stylesheet" href="https://bootswatch.com/4/cerulean/bootstrap.css">
        <link href="{{ asset('sass/style.css') }}" rel="stylesheet"/>
        {% block stylesheets %}{% endblock %}

        {% block javascripts %}{% endblock %}
    </head>
    <body>
        <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
            <a class="navbar-brand" href="{{ path('home') }}">Project Association</a>
            <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
          
            <div class="collapse navbar-collapse" id="navbarColor01">
              <ul class="navbar-nav mr-auto">
                <li class="nav-item ">
                  <a class="nav-link" href="{{ path('blog') }}">Annonces
                  </a>
                </li>
                {% if not app.user %}
                {% else %}
                <li class="nav-item">
                  <a class="nav-link" href="{{ path('blog_annonce') }}">Créer une annonce</a>
                </li>
                {% endif%}
                {% if not app.user %} 
                 <li class="nav-item">
                  <a class="nav-link" href="{{ path('security_registration')}}">S'inscrire</a>
                </li>
                {% else %}
                {% endif%}

                <li class="nav-item">
                  <a class="nav-link" href="#">Contact</a>
                </li>

                <li class="nav-item">
                  <a class="nav-link" href="{{ path('user') }}">Profil</a>
                </li>

              </ul>

              {% if not app.user %}
              <form class="form-inline my-2 my-lg-0">
                <a class="btn btn-light" href="{{ path('security_login')}}">Connexion</a>
              </form>
              {% else %}
              <form class="form-inline my-2 my-lg-0">
                <a class="btn btn-light" href="{{ path('security_logout')}}">Déconnexion</a>
              </form>
              {% endif%}
            </div>
          </nav>

          {% block body %}{% endblock %}

          <footer class="bg-primary">
            <div class="bg-primary footer">
                <div class="first_block">
                    <ul>
                        <li><i class="fas fa-map-marked"></i> 12 rue des radis maudits</li>
                        <li><i class="fas fa-phone"></i> 0789899772</li>
                    </ul></div>
                <div class="second_block"></div>
            </div>
                <p class="text-center">© 2020 Copyright: Parisot Bryan 2021</p>
            </footer>
    </body>
</html>
