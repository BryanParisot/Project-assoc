{% extends 'base.html.twig'%} 

{% block body %}
<div class="picture">
  <h1>{{ title }}</h1>
</div>


<div class="container">
    <form action="{{ path('security_login') }}" method="POST">
        <div class="form-group">
            <input placeholder="adresse email..." type="text" required name="_username" class="form-control mb-4" />
            <input placeholder="mot de passe..." type="password" required name="_password" class="form-control" />
        
             <div class="form-check mt-2">
                <input class="form-check-input" type="checkbox" value="" id="flexCheckDefault">
                <label class="form-check-label" for="flexCheckDefault">Rester connecté</label>
             </div>
        </div> 

        <div class="form-group d-flex justify-content-center">
            <button class="btn btn-primary d-flex justify-content-center" type="submit">Se connecter</button>
        </div>
    
    </form>
</div>
{% endblock %}
