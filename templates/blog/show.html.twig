{% extends 'base.html.twig'%}

{% block body %}
<div class="picture">
    <h1>{{ title }}</h1>
</div>
<h3 class="text-center"> {{ article.prenom}}</h3>


<div class="container d-flex justify-content-center">
    {% for image in article.images %}
    <img src="{{ asset('/uploads/' ~ image.name) }}" width="800" alt="nnn">
    {% endfor%}

</div>

<div class="container mt-5 mb-5">
    <ul class="list-group">
        
        <li class="list-group-item d-flex"><strong>Type : </strong> <p class="ml-2">{{ article.categorie.titre }}</p></li> 
        <li class="list-group-item d-flex"><strong>Race :</strong> <p class="ml-2">{{ article.race}}</p></li> 
        <li class="list-group-item d-flex"><strong>Sexe :</strong> <p class="ml-2">{{ article.sexe}}</p></li> 
        <li class="list-group-item d-flex"><strong>Age :</strong> <p class="ml-2">{{ article.age}}ans</p></li> 
        <li class="list-group-item d-flex"><strong>Grand descriptif:</strong> <p class="ml-2">{{ article.granddescriptif | raw}}</p></li>
        <li class="list-group-item d-flex"><strong>Entente :</strong> <p class="ml-2"> {{ article.entente}}</p></li> 
        <li class="list-group-item d-flex"><strong>Ville :</strong> <p class="ml-2">Nancy</p></li> 
        <li class="list-group-item d-flex"><strong>Adresse :</strong> <p class="ml-2">12 rue des pommes de terres</p></li> 
        <li class="list-group-item d-flex"><strong>Numéro :</strong> <p class="ml-2">0909080909</p></li> 
      </ul>
      {% for image in article.images %}
      <a href="{{ path('blog_edit', {'id': article.id}) }}" class="btn btn-primary mt-4">Modifier l'annonce</a>
      {% endfor%}

    </div>




{% endblock%}